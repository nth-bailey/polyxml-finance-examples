// @generated by PolyXML Compiler (https://github.com/polyxml/PolyXML)
// Target: Modern C++20/C++23 (Header-Only)
#pragma once

#include <regex>
#include <concepts>
#include <cstdint>
#include <memory>
#include <optional>
#include <string>
#include <string_view>
#include <variant>
#include <vector>

namespace polyxml::generated {

// Canonical C++20 pattern matching visitor for std::variant
template <class... Ts>
struct overloaded : Ts... {
    using Ts::operator()...;
};
template <class... Ts>
overloaded(Ts...) -> overloaded<Ts...>;

// Concept validating PolyXML C++20 equality comparable value types
template <typename T>
concept XmlModel = requires(T a) {
    { a == a } -> std::convertible_to<bool>;
};

// Forward declarations
struct AccountIdentification;
struct ActiveOrHistoricCurrencyAndAmount;
struct BranchAndFinancialInstitutionIdentification;
struct CashAccount;
struct ClearingSystemMemberIdentification;
struct CreditTransferTransactionInformation;
struct FiToFiCustomerCreditTransfer;
struct FinancialInstitutionIdentification;
struct GroupHeader;
struct PartyIdentification;
struct PaymentIdentification;
struct PostalAddress;
struct RemittanceInformation;
struct SettlementInstruction;

using ActiveCurrencyCode = std::string;

[[nodiscard]] inline bool validate_ActiveCurrencyCode_patterns(std::string_view value) noexcept {
    try {
        static const std::regex pattern_0("[A-Z]{3,3}");
        if (!std::regex_match(value.begin(), value.end(), pattern_0)) return false;
        return true;
    } catch (const std::regex_error&) { return false; }
}

enum class ChargeBearerType {
    Debt,
    Cred,
    Shar,
    Slev,
};

[[nodiscard]] inline constexpr std::string_view to_string(ChargeBearerType value) noexcept {
    switch (value) {
        case ChargeBearerType::Debt: return "DEBT";
        case ChargeBearerType::Cred: return "CRED";
        case ChargeBearerType::Shar: return "SHAR";
        case ChargeBearerType::Slev: return "SLEV";
    }
    return "";
}

[[nodiscard]] inline std::optional<ChargeBearerType> charge_bearer_type_from_string(std::string_view s) noexcept {
    if (s == "DEBT") return ChargeBearerType::Debt;
    if (s == "CRED") return ChargeBearerType::Cred;
    if (s == "SHAR") return ChargeBearerType::Shar;
    if (s == "SLEV") return ChargeBearerType::Slev;
    return std::nullopt;
}

enum class SettlementMethodCode {
    Inda,
    Inga,
    Clrg,
};

[[nodiscard]] inline constexpr std::string_view to_string(SettlementMethodCode value) noexcept {
    switch (value) {
        case SettlementMethodCode::Inda: return "INDA";
        case SettlementMethodCode::Inga: return "INGA";
        case SettlementMethodCode::Clrg: return "CLRG";
    }
    return "";
}

[[nodiscard]] inline std::optional<SettlementMethodCode> settlement_method_code_from_string(std::string_view s) noexcept {
    if (s == "INDA") return SettlementMethodCode::Inda;
    if (s == "INGA") return SettlementMethodCode::Inga;
    if (s == "CLRG") return SettlementMethodCode::Clrg;
    return std::nullopt;
}

struct AccountIdentification {
    std::optional<std::string> iban = std::nullopt;
    std::optional<std::string> proprietary_account = std::nullopt;

    bool operator==(const AccountIdentification&) const = default;

    [[nodiscard]] bool validate() const noexcept {
        return true;
    }
};

struct ClearingSystemMemberIdentification {
    std::string clearing_system_id = {};
    std::string member_id = {};

    bool operator==(const ClearingSystemMemberIdentification&) const = default;

    [[nodiscard]] bool validate() const noexcept {
        return true;
    }
};

struct PaymentIdentification {
    std::string instruction_id = {};
    std::string end_to_end_id = {};
    std::string transaction_id = {};
    std::string uetr = {};

    bool operator==(const PaymentIdentification&) const = default;

    [[nodiscard]] bool validate() const noexcept {
        return true;
    }
};

struct PostalAddress {
    std::optional<std::string> street_name = std::nullopt;
    std::optional<std::string> building_number = std::nullopt;
    std::optional<std::string> post_code = std::nullopt;
    std::string town_name = {};
    std::string country = {};

    bool operator==(const PostalAddress&) const = default;

    [[nodiscard]] bool validate() const noexcept {
        return true;
    }
};

struct RemittanceInformation {
    std::optional<std::string> unstructured = std::nullopt;

    bool operator==(const RemittanceInformation&) const = default;

    [[nodiscard]] bool validate() const noexcept {
        return true;
    }
};

struct ActiveOrHistoricCurrencyAndAmount {
    ActiveCurrencyCode currency = {};
    double value = 0.0;

    bool operator==(const ActiveOrHistoricCurrencyAndAmount&) const = default;

    [[nodiscard]] bool validate() const noexcept {
        if (!validate_ActiveCurrencyCode_patterns(currency)) return false;
        return true;
    }
};

struct CashAccount {
    AccountIdentification id = {};
    std::optional<ActiveCurrencyCode> currency = std::nullopt;
    std::optional<std::string> name = std::nullopt;

    bool operator==(const CashAccount&) const = default;

    [[nodiscard]] bool validate() const noexcept {
        if (currency.has_value()) { if (!validate_ActiveCurrencyCode_patterns(*currency)) return false; }
        return true;
    }
};

struct FinancialInstitutionIdentification {
    std::optional<std::string> bicfi = std::nullopt;
    std::optional<ClearingSystemMemberIdentification> clearing_system_member_id = std::nullopt;
    std::optional<std::string> name = std::nullopt;

    bool operator==(const FinancialInstitutionIdentification&) const = default;

    [[nodiscard]] bool validate() const noexcept {
        return true;
    }
};

struct PartyIdentification {
    std::string name = {};
    std::optional<PostalAddress> postal_address = std::nullopt;
    std::optional<std::string> country_of_residence = std::nullopt;

    bool operator==(const PartyIdentification&) const = default;

    [[nodiscard]] bool validate() const noexcept {
        return true;
    }
};

struct SettlementInstruction {
    SettlementMethodCode settlement_method = {};
    std::optional<std::string> clearing_system = std::nullopt;

    bool operator==(const SettlementInstruction&) const = default;

    [[nodiscard]] bool validate() const noexcept {
        return true;
    }
};

struct BranchAndFinancialInstitutionIdentification {
    FinancialInstitutionIdentification fin_instn_id = {};

    bool operator==(const BranchAndFinancialInstitutionIdentification&) const = default;

    [[nodiscard]] bool validate() const noexcept {
        return true;
    }
};

struct GroupHeader {
    std::string msg_id = {};
    std::string cre_dt_tm = {};
    std::int32_t nb_of_txs = 0;
    SettlementInstruction sttlm_inf = {};

    bool operator==(const GroupHeader&) const = default;

    [[nodiscard]] bool validate() const noexcept {
        return true;
    }
};

struct CreditTransferTransactionInformation {
    PaymentIdentification pmt_id = {};
    ActiveOrHistoricCurrencyAndAmount intr_bk_sttlm_amt = {};
    std::string intr_bk_sttlm_dt = {};
    ChargeBearerType charge_bearer = {};
    PartyIdentification dbtr = {};
    CashAccount dbtr_acct = {};
    BranchAndFinancialInstitutionIdentification dbtr_agt = {};
    BranchAndFinancialInstitutionIdentification cdtr_agt = {};
    PartyIdentification cdtr = {};
    CashAccount cdtr_acct = {};
    std::optional<std::string> purpose = std::nullopt;
    std::optional<RemittanceInformation> rmt_inf = std::nullopt;

    bool operator==(const CreditTransferTransactionInformation&) const = default;

    [[nodiscard]] bool validate() const noexcept {
        return true;
    }
};

struct FiToFiCustomerCreditTransfer {
    GroupHeader grp_hdr = {};
    std::vector<CreditTransferTransactionInformation> cdt_trf_tx_inf = {};

    bool operator==(const FiToFiCustomerCreditTransfer&) const = default;

    [[nodiscard]] bool validate() const noexcept {
        return true;
    }
};


// Root XML Element Type Aliases
using Document = FiToFiCustomerCreditTransfer;

} // namespace polyxml::generated
